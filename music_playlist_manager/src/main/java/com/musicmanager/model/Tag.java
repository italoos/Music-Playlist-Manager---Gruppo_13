package com.musicmanager.model;

/**
 * Tag assegnabili alle tracce per evidenziarne caratteristiche specifiche.
 */
public enum Tag {
    /** Brano contrassegnato come preferito. */
    FAVOURITE,

    /** Brano con contenuto esplicito. */
    EXPLICIT,

    /** Brano indicato come nuova uscita. */
    NEW_RELEASE
}
